{
  "name": "boutique-catalog",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Catálogo público + panel admin (Supabase). Vanilla JS, sin build step.",
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^2.1.8",
    "jsdom": "^25.0.1",
    "@vitest/coverage-v8": "^2.1.8"
  }
}
